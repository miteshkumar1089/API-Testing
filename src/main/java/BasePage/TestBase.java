package BasePage;

import APIHelper.DataFactory;
import APIHelper.GenerateToken;
import org.testng.annotations.BeforeMethod;

public class TestBase {
    public static DataFactory data = new DataFactory();
    public static GenerateToken generateToken=new GenerateToken();

   // @BeforeMethod
}
